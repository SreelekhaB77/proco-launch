/*$(document).ready(function() {
	$('#change_pswd').on('show.bs.modal', function (e) {
		var link = $(e.relatedTarget);
		   $(this).find(".change-pwd").load(link.attr("href"));
		});
	
	
	$('#change_pswd').on('hidden.bs.modal', function() {
		$('#pwdform').data('bootstrapValidator').resetForm(true);
		});
		
});*/
